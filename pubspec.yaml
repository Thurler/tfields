name: tfields
description: "Thurler Flutter Fields and Widgets"
version: 2.1.1

publish_to: none

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  agattp:
    git:
      url: https://github.com/anlix-io/agattp
      ref: v1.0.0
  flutter:
    sdk: flutter
  meta: 1.16.0
  provider: 6.1.4
  url_launcher: 6.1.11

dev_dependencies:
  flutter_lints: ^4.0.0
